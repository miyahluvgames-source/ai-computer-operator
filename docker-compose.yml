services:
  operator:
    build: .
    ports:
      - "9222:9222"
    volumes:
      - ./artifacts:/app/artifacts
    command: ["--plan", "examples/example-plan.json", "--cdp-port", "9222", "--out", "artifacts"]
